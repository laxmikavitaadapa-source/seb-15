Popular or Most used GIT commands

/* Initialize Empty git repository 

First time developer or new developer*/

git init.

git config --global user.name "Hemanth naga sai".

git config --global user email "2400030992@kluniversity.in".

"Add files"/

Add commit message "/

git add.

Agit commit -m "First project

Move to Branch name main

git branch -M main.
 
git remote add origin.

git push -U main.

git checkout.

git log-project or tracks the history.
