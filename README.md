git init.

git config --global user.name "A.hemanth naga sai"

git config --global user.email "24000320992@kluniversity.in"

git add .

git commit -m "commited"

git branch -M main
 
git remote add origin.

git push -U main.

git checkout

git log
